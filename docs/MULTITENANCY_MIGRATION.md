# 🔐 Инструкция по миграции на мультитенантность

**Дата**: 2024-11-01  
**Версия**: 1.0.0  
**Критичность**: ВЫСОКАЯ

---

## 📋 Обзор изменений

Реализована жесткая мультитенантность (SaaS модель), где каждый администратор видит **только свои проекты и пользователей**. Данные полностью изолированы по владельцу.

### Изменения в БД:
- Добавлено поле `owner_id` в таблицу `projects`
- Связь с таблицей `admin_accounts` через внешний ключ
- Все существующие проекты **не будут** иметь владельца до ручной миграции

### Изменения в API:
- `GET /api/projects` - теперь возвращает только проекты владельца
- `POST /api/projects` - автоматически привязывает проект к текущему админу
- Все endpoints `/api/projects/[id]/*` требуют проверки доступа
- Возврат **403 Forbidden** при попытке доступа к чужому проекту

---

## ⚠️ ВАЖНО: Что делать сейчас

### Если у вас уже есть проекты в БД

Вам **ОБЯЗАТЕЛЬНО** нужно выполнить миграцию данных, иначе:
1. ❌ Все существующие проекты будут видны всем админам (опасно!)
2. ❌ Новые админы не смогут создать проект
3. ❌ Данные могут быть удалены при удалении админов

---

## 🚀 Пошаговая инструкция

### Шаг 1: Проверьте текущее состояние

```bash
# Подключитесь к БД и проверьте количество проектов без владельца
npm run db:generate
npx prisma studio
```

Откройте таблицу `projects` и убедитесь, что колонка `owner_id` NULL для всех записей.

### Шаг 2: Создайте первого администратора

Если у вас еще нет админов, зарегистрируйтесь через UI:
- Откройте `/auth/sign-up`
- Введите email и пароль
- ВАЖНО: Запишите эти данные!

Или создайте админа через API:
```bash
curl -X POST http://localhost:5006/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@mycompany.com",
    "password": "SecurePass123!",
    "role": "SUPERADMIN"
  }'
```

### Шаг 3: Запустите скрипт миграции

```bash
# Привязать все проекты к созданному админу
npm run migrate-owners migrate admin@mycompany.com SecurePass123!
```

**Что делает скрипт:**
1. Находит всех админов в БД (если нет - создает дефолтного)
2. Привязывает все проекты без `owner_id` к указанному админу
3. Показывает статистику миграции

**Ожидаемый результат:**
```
🚀 Начинаем миграцию владельцев проектов...

📊 Найдено проектов без владельца: 5
✅ Привязан проект: Моя Компания (cmabc123...)
✅ Привязан проект: Тестовый проект (cmdef456...)
...

🎉 Миграция завершена!
📈 Статистика миграции:
   • Всего проектов обработано: 5
   • Успешно привязано: 5
   • Ошибок: 0
```

### Шаг 4: Проверьте целостность данных

```bash
# Проверить что все проекты имеют владельца
npm run migrate-owners validate
```

**Должно вывести:**
```
🔍 Проверка целостности данных после миграции...

📊 Всего проектов: 5
📊 Проектов без владельца: 0
📊 Всего администраторов: 1
✅ Проверка целостности пройдена успешно!
```

### Шаг 5: Проверьте изоляцию в UI

1. Зайдите под созданным админом
2. Откройте `/dashboard/projects`
3. Убедитесь, что видите **только свои проекты**
4. Создайте еще одного админа через `/auth/sign-up` (другой email)
5. Зайдите под вторым админом
6. Убедитесь, что вы видите **пустой список проектов** ✅

### Шаг 6: Создайте проект для второго админа

1. Войдите под вторым админом
2. Создайте новый проект через UI
3. Убедитесь, что проект отображается **только у второго админа**
4. Выйдите и зайдите под первым админом
5. Убедитесь, что **не видите** проект второго админа ✅

---

## 🔧 Дополнительные команды скрипта

### Создать нового админа
```bash
npm run migrate-owners create-admin admin@test.com Test123!
```

### Проверить без миграции
```bash
npm run migrate-owners validate
```

### Откат миграции (ОПАСНО!)
```bash
npm run migrate-owners rollback
```
**ВНИМАНИЕ**: Это отвяжет ВСЕ проекты от владельцев!

---

## 🐛 Решение проблем

### Проблема: "Migration failed to apply"

**Решение**: Примените миграцию вручную:
```bash
npx prisma migrate deploy
npx prisma db execute --stdin --schema=prisma/schema.prisma < prisma/migrations/20251101225630_add_project_owner/migration.sql
```

### Проблема: "Cannot read property 'sub' of null"

**Решение**: Убедитесь, что вы залогинены через `/auth/sign-in` перед выполнением миграции.

### Проблема: "All projects visible to everyone"

**Решение**: Проверьте, что миграция применена:
```bash
npm run migrate-owners validate
```

Если проекты без владельца - запустите миграцию:
```bash
npm run migrate-owners migrate admin@example.com SecurePass123!
```

---

## 📝 Что изменилось для разработчиков

### В коде API теперь:

```typescript
// ДО (уязвимо):
export async function GET(request: NextRequest) {
  const projects = await ProjectService.getProjects();
  return projects; // ВСЕ проекты!
}

// ПОСЛЕ (безопасно):
export async function GET(request: NextRequest) {
  const admin = await getCurrentAdmin();
  if (!admin) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  
  const projects = await ProjectService.getProjects(page, limit, admin.sub);
  return projects; // Только проекты владельца
}
```

### Для новых API endpoints:

Всегда добавляйте проверку доступа:
```typescript
import { getCurrentAdmin } from '@/lib/auth';
import { ProjectService } from '@/lib/services/project.service';

export async function GET(request: NextRequest, context: { params: { id: string } }) {
  const admin = await getCurrentAdmin();
  if (!admin) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }
  
  await ProjectService.verifyProjectAccess(id, admin.sub);
  
  // ... ваш код
}
```

---

## 🔒 Безопасность

### Что защищено:
✅ Все CRUD операции с проектами  
✅ Управление пользователями  
✅ Настройки бота  
✅ Workflow и сценарии  
✅ Уведомления  
✅ Статистика и аналитика  

### Что НЕ защищено (публичные API):
❌ `/api/webhook/*` - работает по `webhookSecret`  
❌ `/api/projects/:id/users/balance` - публичный для Tilda  
❌ `GET /api/projects/:id/bot` - публичный для виджетов  

---

## ✅ Чеклист после миграции

- [ ] Миграция БД применена
- [ ] Prisma Client сгенерирован
- [ ] Первый админ создан
- [ ] Скрипт миграции выполнен успешно
- [ ] Все проекты имеют `owner_id`
- [ ] Проверка целостности пройдена
- [ ] Тестовый доступ изолирован (2 админа видят разные проекты)
- [ ] Webhook API работает (проверьте через `/api/webhook/:secret`)
- [ ] Публичные API работают
- [ ] Новые проекты создаются с правильным `owner_id`

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs db`
2. Проверьте миграции: `npm run migrate-owners validate`
3. Посмотрите документацию: `docs/database-schema.md`
4. Создайте issue с детальным описанием

---

**Дата создания**: 2024-11-01  
**Автор**: AI Assistant + User  
**Проект**: SaaS Bonus System

