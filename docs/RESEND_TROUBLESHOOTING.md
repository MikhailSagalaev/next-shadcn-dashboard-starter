# 🔧 Диагностика проблем с Resend после деплоя

## 🚨 Проблема: Письма перестали приходить после деплоя

### Шаг 1: Проверка переменных окружения в PM2

PM2 **не загружает** переменные из `.env` файла автоматически. Нужно явно указать их.

**Проверьте текущие переменные в PM2:**
```bash
# Просмотр всех переменных окружения процесса
pm2 env 0

# Или проверка конкретного процесса
pm2 describe bonus-app | grep -A 20 "env:"
```

**Проверьте, есть ли RESEND_API_KEY:**
```bash
pm2 env 0 | grep RESEND
```

### Шаг 2: Установка переменных в PM2

**Вариант 1: Через ecosystem.config.cjs (рекомендуется)**

Убедитесь, что в `ecosystem.config.cjs` загружается `.env` файл:

```javascript
const path = require('path');
require('dotenv').config({ path: path.join(__dirname, '.env') });
```

**Вариант 2: Явное указание в ecosystem.config.cjs**

```javascript
env: {
  RESEND_API_KEY: process.env.RESEND_API_KEY,
  RESEND_FROM_EMAIL: process.env.RESEND_FROM_EMAIL,
  // ... другие переменные
  NODE_ENV: 'production',
  PORT: '3000'
}
```

**Вариант 3: Установка через PM2 команды**

```bash
# Установить переменную для процесса
pm2 set bonus-app RESEND_API_KEY "re_your_key_here"
pm2 set bonus-app RESEND_FROM_EMAIL "noreply@yourdomain.com"

# Перезапустить с обновленными переменными
pm2 restart bonus-app --update-env
```

### Шаг 3: Перезапуск PM2 с загрузкой .env

```bash
# Остановить процесс
pm2 stop bonus-app

# Удалить из PM2
pm2 delete bonus-app

# Запустить заново с загрузкой .env
cd /opt/next-shadcn-dashboard-starter
pm2 start ecosystem.config.cjs --update-env

# Или если используете yarn script
pm2 restart bonus-app --update-env
```

### Шаг 4: Проверка логов

**Ищите в логах PM2:**

```bash
# Просмотр последних 100 строк логов
pm2 logs bonus-app --lines 100

# Поиск упоминаний Resend
pm2 logs bonus-app --lines 200 | grep -i resend

# Поиск ошибок email
pm2 logs bonus-app --lines 200 | grep -i email
```

**Что искать:**
- ✅ `📧 Email отправлен успешно через Resend` — всё работает
- ⚠️ `RESEND_API_KEY не установлен, используется заглушка` — переменная не загружена
- ❌ `Ошибка отправки email через Resend` — проблема с API ключом или настройками
- ❌ `Invalid API Key` — неверный ключ
- ❌ `Forbidden` — проблема с доменом или правами

### Шаг 5: Диагностический скрипт

Запустите скрипт для проверки конфигурации:

```bash
cd /opt/next-shadcn-dashboard-starter
npx tsx scripts/check-resend-config.ts
```

Скрипт покажет:
- Установлен ли RESEND_API_KEY
- Установлен ли RESEND_FROM_EMAIL
- Правильный ли формат ключа (начинается с `re_`)
- Работает ли клиент Resend

### Шаг 6: Ручная проверка через тестовое письмо

1. Откройте `/dashboard/settings`
2. Перейдите на вкладку "Уведомления"
3. Укажите ваш email в поле "Email для уведомлений"
4. Включите "Email уведомления" если выключено
5. Нажмите "Отправить тестовое письмо"

Проверьте логи PM2 после нажатия:
```bash
pm2 logs bonus-app --lines 50
```

### Шаг 7: Проверка переменных в .env файле

Убедитесь, что в `.env` файле на сервере есть:

```bash
cat /opt/next-shadcn-dashboard-starter/.env | grep RESEND
```

Должно быть:
```env
RESEND_API_KEY=re_xxxxxxxxxxxxx
RESEND_FROM_EMAIL=noreply@yourdomain.com
```

**Важно:**
- API ключ должен начинаться с `re_`
- Email должен быть верифицирован в Resend Dashboard (для кастомных доменов)
- Для тестирования можно использовать `noreply@resend.dev` (встроенный домен Resend)

### Шаг 8: Проверка в Resend Dashboard

1. Зайдите в [Resend Dashboard](https://resend.com/dashboard)
2. Перейдите в **"API Keys"**
3. Убедитесь, что ключ активен и имеет права `sending_access`
4. Перейдите в **"Emails"** → посмотрите последние отправки
5. Если есть ошибки — они будут показаны там

### Шаг 9: Проверка домена (если используете кастомный)

Если используете кастомный домен (не `@resend.dev`):

1. В Resend Dashboard → **"Domains"**
2. Убедитесь, что домен верифицирован (зелёная галочка)
3. Проверьте DNS записи (SPF, DKIM, DMARC)

## 🔄 Быстрое исправление

**Если переменные не загружены в PM2:**

```bash
# 1. Отредактируйте .env файл (если нужно)
nano /opt/next-shadcn-dashboard-starter/.env

# 2. Убедитесь что RESEND_API_KEY и RESEND_FROM_EMAIL установлены

# 3. Остановите и удалите процесс
pm2 stop bonus-app
pm2 delete bonus-app

# 4. Запустите заново
cd /opt/next-shadcn-dashboard-starter
pm2 start ecosystem.config.cjs --update-env
pm2 save

# 5. Проверьте переменные
pm2 env 0 | grep RESEND

# 6. Проверьте логи
pm2 logs bonus-app --lines 50
```

## 📋 Чек-лист

- [ ] Переменные `RESEND_API_KEY` и `RESEND_FROM_EMAIL` есть в `.env` файле
- [ ] Переменные загружены в PM2 процесс (`pm2 env 0 | grep RESEND`)
- [ ] `ecosystem.config.cjs` содержит `require('dotenv').config()`
- [ ] PM2 перезапущен с флагом `--update-env`
- [ ] В логах нет ошибок "RESEND_API_KEY не установлен"
- [ ] API ключ активен в Resend Dashboard
- [ ] Домен верифицирован (если кастомный)
- [ ] Тестовое письмо отправляется успешно

## 🆘 Если ничего не помогает

1. **Создайте новый API ключ в Resend Dashboard**
2. **Обновите `.env` файл**
3. **Перезапустите PM2:**
   ```bash
   pm2 restart bonus-app --update-env
   ```
4. **Проверьте логи:**
   ```bash
   pm2 logs bonus-app --lines 100 | grep -i "email\|resend"
   ```

---

**💡 Совет:** Добавьте проверку переменных в startup скрипт, чтобы видеть предупреждения при запуске, если что-то не настроено.

