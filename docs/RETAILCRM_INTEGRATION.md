# 📡 Интеграция с RetailCRM

## Обзор

Интеграция с RetailCRM позволяет синхронизировать данные о заказах и клиентах между системой бонусных программ и RetailCRM.

## Архитектура

### 1. Компоненты системы

- **RetailCrmClientService** (`src/lib/services/retailcrm-client.service.ts`) - API клиент для работы с RetailCRM
- **RetailCrmSyncService** (`src/lib/services/retailcrm-sync.service.ts`) - Сервис управления синхронизацией
- **retailCrmSyncQueue** (`src/lib/queues/retailcrm-sync.queue.ts`) - Bull очередь для асинхронной обработки
- **RetailCrmIntegration** (модель БД) - Хранение настроек интеграции

### 2. Настройка интеграции

#### UI компонент
- **Путь**: `/dashboard/projects/[id]/retailcrm`
- **Компонент**: `src/features/retailcrm/components/retailcrm-integration-view.tsx`

#### Настройки
- **API URL** - URL вашего RetailCRM (например: `https://your-store.retailcrm.ru`)
- **API Key** - API ключ из RetailCRM
- **isActive** - Включить/выключить интеграцию
- **syncOrders** - Синхронизировать заказы
- **syncCustomers** - Синхронизировать клиентов

### 3. Процесс синхронизации

#### Синхронизация заказов
1. Получение заказов из RetailCRM через API
2. Поиск пользователя по email/телефону
3. Проверка существования заказа (по `orderNumber`)
4. Обновление или создание заказа
5. Маппинг статусов RetailCRM → система

#### Синхронизация клиентов
1. Получение клиентов из RetailCRM через API
2. Поиск пользователя по email/телефону
3. Создание нового пользователя или обновление существующего
4. Сохранение данных (email, phone, firstName, lastName)

### 4. Маппинг статусов

| RetailCRM | Система |
|-----------|---------|
| `new` | `PENDING` |
| `acceptance` | `CONFIRMED` |
| `assembling` | `PROCESSING` |
| `delivery` | `SHIPPED` |
| `complete` | `DELIVERED` |
| `cancel` | `CANCELLED` |
| `refund` | `REFUNDED` |

## API методы

### RetailCrmClientService

```typescript
// Создание клиента
const client = await RetailCrmClientService.create(projectId);

// Получение заказов
const orders = await client.getOrders({ sinceId, limit: 100 });

// Получение клиентов
const customers = await client.getCustomers({ sinceId, limit: 100 });

// Создание заказа
const order = await client.createOrder(orderData);

// Обновление заказа
const updatedOrder = await client.updateOrder(orderId, orderData);

// Создание клиента
const customer = await client.createCustomer(customerData);

// Обновление клиента
const updatedCustomer = await client.updateCustomer(customerId, customerData);

// Проверка соединения
const isConnected = await client.testConnection();
```

### RetailCrmSyncService

```typescript
// Синхронизация заказов
await RetailCrmSyncService.syncOrders(projectId, sinceId);

// Синхронизация клиентов
await RetailCrmSyncService.syncCustomers(projectId, sinceId);

// Полная синхронизация
await RetailCrmSyncService.syncAll(projectId);

// Периодическая синхронизация
await RetailCrmSyncService.schedulePeriodicSync(projectId, 60); // каждые 60 минут
```

## Очередь Bull

### Типы задач

- `sync_orders` - Синхронизация заказов
- `sync_customers` - Синхронизация клиентов
- `sync_order` - Синхронизация одного заказа
- `sync_customer` - Синхронизация одного клиента

### Настройки очереди

- **Повторы**: 3 попытки
- **Backoff**: Экспоненциальный (5 секунд)
- **Redis**: Используется для хранения очереди

## Использование

### 1. Настройка интеграции

1. Перейдите на `/dashboard/projects/[id]/retailcrm`
2. Введите API URL вашего RetailCRM
3. Введите API Key
4. Включите интеграцию
5. Выберите, что синхронизировать (заказы/клиенты)
6. Сохраните настройки

### 2. Запуск синхронизации

#### Ручной запуск (через код)
```typescript
import { RetailCrmSyncService } from '@/lib/services/retailcrm-sync.service';

// Синхронизация заказов
await RetailCrmSyncService.syncOrders(projectId);

// Синхронизация клиентов
await RetailCrmSyncService.syncCustomers(projectId);

// Полная синхронизация
await RetailCrmSyncService.syncAll(projectId);
```

#### Периодическая синхронизация
```typescript
// Настройка синхронизации каждые 60 минут
await RetailCrmSyncService.schedulePeriodicSync(projectId, 60);
```

### 3. Мониторинг синхронизации

- **lastSyncAt** - Время последней синхронизации (обновляется автоматически)
- **Логи** - Все операции логируются через `logger`
- **Ошибки** - Обрабатываются через Bull queue (повторы, backoff)

## Требования

- **Redis** - Для работы очереди Bull
- **RetailCRM API** - API ключ и URL
- **Настроенная интеграция** - API URL и API Key должны быть сохранены

## Ограничения

- Синхронизация только из RetailCRM → система (односторонняя)
- Нет автоматической синхронизации при изменении данных в системе
- Нет вебхуков от RetailCRM (только периодическая синхронизация)
- Нет UI для мониторинга синхронизации (только через логи)

## Планы развития

- [ ] Добавить API endpoint для ручного запуска синхронизации
- [ ] Добавить UI для мониторинга синхронизации
- [ ] Добавить вебхуки от RetailCRM для реального времени
- [ ] Добавить двустороннюю синхронизацию
- [ ] Добавить настройку маппинга статусов
- [ ] Добавить фильтры синхронизации (по дате, статусу и т.д.)

## Troubleshooting

### Проблема: Синхронизация не запускается

**Решение**:
1. Проверьте, что интеграция активна (`isActive = true`)
2. Проверьте, что включена синхронизация заказов/клиентов
3. Проверьте, что Redis запущен
4. Проверьте логи на наличие ошибок

### Проблема: Ошибка подключения к RetailCRM

**Решение**:
1. Проверьте правильность API URL
2. Проверьте правильность API Key
3. Проверьте доступность RetailCRM API
4. Используйте `testConnection()` для проверки

### Проблема: Заказы не синхронизируются

**Решение**:
1. Проверьте, что заказы существуют в RetailCRM
2. Проверьте, что `syncOrders = true`
3. Проверьте логи на наличие ошибок
4. Проверьте, что пользователи найдены по email/телефону

## Дополнительная информация

- **Документация RetailCRM API**: https://docs.retailcrm.ru/
- **Модель БД**: `prisma/schema.prisma` (RetailCrmIntegration)
- **API Endpoint**: `/api/projects/[id]/retailcrm`

