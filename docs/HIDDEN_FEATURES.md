# 👁️ Скрытый функционал

## Обзор

Следующие функции полностью реализованы и работают, но **скрыты в навигации UI**:

- ✅ **Заказы** (Orders) - `/dashboard/projects/[id]/orders`
- ✅ **Товары** (Products) - `/dashboard/projects/[id]/products`
- ✅ **RetailCRM интеграция** - `/dashboard/projects/[id]/retailcrm`
- ✅ **Сегменты** (Segments) - `/dashboard/projects/[id]/segments`
- ✅ **Рассылки** (Mailings) - `/dashboard/projects/[id]/mailings`
- ✅ **Чаты** (Chats) - `/dashboard/projects/[id]/chats`

## Статус

- **Backend**: ✅ Полностью готов (100%)
- **API**: ✅ Все endpoints работают
- **UI компоненты**: ✅ Полностью реализованы
- **База данных**: ✅ Все таблицы созданы
- **Навигация**: ⚠️ Скрыта (закомментирована)

## Где скрыто

### 1. Навигация проекта (Настройки проекта)

**Файл**: `src/features/projects/components/project-settings-view.tsx`

**Скрытые разделы**:
- Раздел "Продажи и аналитика" (строки ~519-580)
- Раздел "Маркетинг" (строки ~547-565)
- Раздел "Коммуникации" (строки ~567-579)

**Как найти**: Ищите комментарий `{/* Новый функционал - СКРЫТ */}`

### 2. Карточки проектов

**Файл**: `src/features/projects/components/projects-view-page.tsx`

**Скрытые кнопки**:
- Кнопка "Заказы" (строки ~248-256)
- Кнопка "Чаты" (строки ~258-266)

**Как найти**: Ищите комментарий `{/* Новый функционал - СКРЫТ */}`

## Как включить функционал

### Вариант 1: Включить все сразу

1. Откройте `src/features/projects/components/project-settings-view.tsx`
2. Найдите блок с комментарием `{/* Новый функционал - СКРЫТ */}`
3. Раскомментируйте все три раздела (удалите `{/* */}`)
4. Откройте `src/features/projects/components/projects-view-page.tsx`
5. Найдите блок с комментарием `{/* Новый функционал - СКРЫТ */}`
6. Раскомментируйте блок с кнопками

### Вариант 2: Включить выборочно

Вы можете раскомментировать только нужные разделы:

**Для включения только заказов и товаров:**
- Раскомментируйте только раздел "Продажи и аналитика" в `project-settings-view.tsx`

**Для включения только сегментов и рассылок:**
- Раскомментируйте только раздел "Маркетинг" в `project-settings-view.tsx`

**Для включения только чатов:**
- Раскомментируйте только раздел "Коммуникации" в `project-settings-view.tsx`

## Прямой доступ

Функционал доступен напрямую по URL, даже если скрыт в навигации:

- `/dashboard/projects/[id]/orders` - Заказы
- `/dashboard/projects/[id]/products` - Товары
- `/dashboard/projects/[id]/retailcrm` - RetailCRM
- `/dashboard/projects/[id]/segments` - Сегменты
- `/dashboard/projects/[id]/mailings` - Рассылки
- `/dashboard/projects/[id]/chats` - Чаты

## Технические детали

### Backend готовность

- ✅ Все сервисы реализованы (`OrderService`, `ProductService`, `SegmentationService`, `MailingService`, `ChatManagerService`, `RetailCrmClientService`, `RetailCrmSyncService`)
- ✅ Все API endpoints работают
- ✅ База данных синхронизирована
- ✅ Миграции применены

### UI готовность

- ✅ Все компоненты созданы
- ✅ Все страницы работают
- ✅ Навигация скрыта (закомментирована)

### Дополнительные функции

- ✅ Визуальный конструктор условий сегментации
- ✅ WYSIWYG редактор для рассылок
- ✅ Real-time чаты через SSE
- ✅ Многоуровневая реферальная система (3 уровня, welcome-бонус, аналитика)

## Примечания

- Функционал полностью рабочий и протестирован
- Скрытие сделано только в UI навигации
- API endpoints доступны и работают
- База данных содержит все необходимые таблицы
- Для включения достаточно раскомментировать соответствующие блоки

## Дата скрытия

**2025-01-30** - Функционал скрыт по запросу пользователя

