# 📊 Таблица всех нод конструктора Workflow

Полный справочник по всем нодам, доступным в конструкторе workflow системы бонусных программ.

## 📋 Содержание

- [Триггеры](#триггеры)
- [Сообщения](#сообщения)
- [Действия](#действия)
- [Логика](#логика)
- [Управление потоком](#управление-потоком)
- [Интеграции](#интеграции)

---

## 🎯 Триггеры

Точки входа в сценарий. Определяют, когда и как запускается workflow.

| Тип ноды | Название | Описание | Ключевые параметры | Цвет |
|----------|----------|----------|-------------------|------|
| `trigger.command` | Команда | Запуск по команде, например /start | `command: string` (например: '/start', '/help') | 🟢 #15803d |
| `trigger.message` | Сообщение | Автозапуск по тексту пользователя | `pattern?: string` (регулярное выражение), `caseSensitive?: boolean` | 🔵 #2563eb |
| `trigger.callback` | Callback | Обработка нажатий на inline-кнопки | `callbackData: string` (callback data от кнопок) | 🟣 #a855f7 |
| `trigger.webhook` | Webhook | Входящий запрос из внешней системы | `webhookUrl: string`, `method?: 'GET' \| 'POST' \| 'PUT' \| 'DELETE'`, `headers?: Record<string, string>` | 🟠 #f97316 |
| `trigger.email` | Email | Запуск по входящему письму | `email: string`, `subject?: string` | 🔴 #db2777 |

---

## 💬 Сообщения

Коммуникация с пользователем. Отправка различных типов контента в Telegram.

| Тип ноды | Название | Описание | Ключевые параметры | Цвет |
|----------|----------|----------|-------------------|------|
| `message` | Текстовое сообщение | Отправка обычного сообщения | `text: string`, `parseMode?: 'Markdown' \| 'HTML' \| 'MarkdownV2'`, `keyboard?: KeyboardConfig` | 🔵 #2563eb |
| `message.keyboard.inline` | Inline клавиатура | Inline-кнопки под сообщением | `text: string`, `buttons: InlineKeyboardButtonConfig[][]`, `parse_mode?: 'HTML' \| 'Markdown' \| 'MarkdownV2'` | 🟣 #7c3aed |
| `message.keyboard.reply` | Reply клавиатура | Меню с reply-кнопками | `text: string`, `buttons: ReplyKeyboardButtonConfig[][]`, `resize_keyboard?: boolean`, `one_time_keyboard?: boolean` | 🟣 #9333ea |
| `message.photo` | Фото | Сообщение с изображением | `photo: string` (fileId или URL), `caption?: string`, `parse_mode?: 'HTML' \| 'Markdown' \| 'MarkdownV2'`, `has_spoiler?: boolean` | 🟡 #f59e0b |
| `message.video` | Видео | Сообщение с видеороликом | `video: string` (fileId или URL), `caption?: string`, `duration?: number`, `width?: number`, `height?: number`, `thumbnail?: string` | 🔴 #db2777 |
| `message.document` | Документ | Отправка файла или документа | `document: string` (fileId или URL), `caption?: string`, `thumbnail?: string`, `disable_content_type_detection?: boolean` | 🔵 #0284c7 |
| `message.edit` | Редактировать | Редактирование отправленного сообщения | `message_id: string \| number`, `text: string`, `parse_mode?: 'HTML' \| 'Markdown' \| 'MarkdownV2'` | ⚫ #6b7280 |
| `message.delete` | Удалить | Удаление сообщения в чате | `message_id: string \| number` | 🔴 #dc2626 |

---

## ⚙️ Действия

Работа с данными и сервисами. Выполнение операций с БД, переменными, API и пользователями.

| Тип ноды | Название | Описание | Ключевые параметры | Цвет |
|----------|----------|----------|-------------------|------|
| `action.api_request` | API запрос | HTTP запрос к внешнему сервису | `url: string`, `method: 'GET' \| 'POST' \| 'PUT' \| 'DELETE' \| 'PATCH'`, `headers?: Record<string, string>`, `body?: any`, `timeout?: number` | 🔵 #2563eb |
| `action.database_query` | База данных | Выполнение безопасного запроса к БД | `query: string` (SQL), `parameters?: any[]`, `resultMapping?: Record<string, string>`, `assignTo?: string` | 🟣 #7c3aed |
| `action.set_variable` | Установить переменную | Сохранение значения в переменную | `variableName: string`, `variableValue: any`, `scope?: 'global' \| 'project' \| 'user' \| 'session'`, `ttl?: number` | 🔴 #db2777 |
| `action.get_variable` | Получить переменную | Чтение значения переменной | `variableName: string`, `defaultValue?: any`, `assignTo?: string` | 🟢 #22c55e |
| `action.request_contact` | Запрос контакта | Ожидание номера телефона пользователя | *(без параметров)* | 🟠 #f97316 |
| `action.send_notification` | Уведомление | Отправка email/telegram/webhook уведомлений | `notificationType: 'email' \| 'telegram' \| 'webhook'`, `recipient: string`, `template?: string`, `priority?: 'low' \| 'normal' \| 'high'` | 🟡 #facc15 |
| `action.check_user_linked` | Проверка связи | Проверка привязки Telegram аккаунта | `userIdentifier: string`, `assignTo: string` | 🔵 #14b8a6 |
| `action.find_user_by_contact` | Поиск по контакту | Поиск пользователя по телефону или email | `contactType: 'phone' \| 'email'`, `contactValue: string`, `assignTo: string` | 🔵 #0891b2 |
| `action.link_telegram_account` | Привязать Telegram | Связка пользователя с Telegram аккаунтом | `telegramId: string`, `contactType: 'phone' \| 'email'`, `contactValue: string` | 🟣 #6366f1 |
| `action.get_user_balance` | Баланс пользователя | Получение текущего баланса бонусов | `userId?: string`, `assignTo: string` | 🟡 #f59e0b |
| `action.menu_command` | Команда меню | *(Зарезервировано)* | *(TBD)* | *(TBD)* |

---

## 🔀 Логика

Ветвления и условия. Контроль потока выполнения на основе данных.

| Тип ноды | Название | Описание | Ключевые параметры | Цвет |
|----------|----------|----------|-------------------|------|
| `condition` | Условие | Ветвление по условию/выражению | `expression?: string` (новый формат), `variable?: string`, `operator?: 'equals' \| 'not_equals' \| 'contains' \| ...`, `value?: any` | 🟠 #f97316 |
| `flow.switch` | Switch | Множественный выбор ветки | `variable: string`, `cases: SwitchCaseConfig[]`, `hasDefault?: boolean` | 🟠 #f97316 |

**Операторы условия:**
- `equals`, `not_equals`, `==`, `!=`, `===`, `!==`
- `contains`, `not_contains`
- `greater`, `less`, `>`, `<`, `>=`, `<=`
- `is_empty`, `is_not_empty`

---

## 🔄 Управление потоком

Контроль последовательности выполнения. Циклы, задержки, подпроцессы.

| Тип ноды | Название | Описание | Ключевые параметры | Цвет |
|----------|----------|----------|-------------------|------|
| `flow.delay` | Задержка | Пауза перед продолжением выполнения | `delayMs: number`, `variableDelay?: string` | 🟡 #facc15 |
| `flow.loop` | Цикл | Повторить действия несколько раз | `type?: 'count' \| 'foreach' \| 'while'`, `iterations: number \| string`, `maxIterations?: number`, `condition?: string` | 🔵 #60a5fa |
| `flow.sub_workflow` | Подпроцесс | Запуск вложенного workflow | `workflowId: string`, `version?: number`, `inputMapping?: Record<string, string>`, `outputMapping?: Record<string, string>` | 🟣 #8b5cf6 |
| `flow.jump` | Переход | Переход на другую ноду | `targetNodeId: string`, `condition?: string` | 🟣 #6366f1 |
| `flow.end` | Завершение | Завершение исполнения | `success?: boolean`, `message?: string` | ⚫ #6b7280 |

**Типы циклов:**
- `count` - выполнить N раз
- `foreach` - для каждого элемента массива
- `while` - пока условие истинно

---

## 🔌 Интеграции

Связь с внешними системами. Webhook'и и аналитика.

| Тип ноды | Название | Описание | Ключевые параметры | Цвет |
|----------|----------|----------|-------------------|------|
| `integration.webhook` | Интеграция (Webhook) | Вызов стороннего Webhook | `url: string`, `method: 'GET' \| 'POST' \| 'PUT' \| 'DELETE'`, `headers?: Record<string, string>`, `body?: any`, `timeout?: number`, `retries?: number` | 🟠 #f97316 |
| `integration.analytics` | Интеграция (Аналитика) | Отправка событий в аналитику | `event: string`, `properties?: Record<string, any>`, `userId?: string` | 🔵 #1d4ed8 |

---

## 📊 Статистика

**Всего нод:** 29

**По категориям:**
- 🎯 Триггеры: 5
- 💬 Сообщения: 8
- ⚙️ Действия: 11
- 🔀 Логика: 2
- 🔄 Управление потоком: 5
- 🔌 Интеграции: 2

---

## 🔍 Примечания

1. **Безопасность:**
   - `action.database_query` использует whitelist разрешенных запросов
   - `action.api_request` имеет защиту от SSRF (URL валидация)
   - Все условия проверяются через безопасный AST evaluator

2. **Переменные:**
   - Поддерживаются scopes: `global`, `project`, `user`, `session`
   - Session переменные могут иметь TTL (Time To Live)
   - Доступ к переменным через шаблоны: `{{variableName}}`

3. **Шаблонизация:**
   - Все текстовые поля поддерживают шаблоны с переменными
   - Формат: `{{variableName}}` или `{{user.name}}`
   - Доступны функции: `upper()`, `lower()`, `format()`, и др.

4. **Обработка ошибок:**
   - Каждая нода может иметь обработчик ошибок
   - Настраивается в `WorkflowSettings.errorHandling`
   - Варианты: `continue`, `retry`, `goto_node`, `stop_workflow`

5. **Rate Limiting:**
   - Применяется автоматически для:
     - Telegram сообщений (30/мин на пользователя)
     - API запросов (60/мин на проект)
     - Workflow execution (100/мин на пользователя)

---

## 📝 Примеры использования

### Пример 1: Простой workflow с командой
```json
{
  "nodes": [
    {
      "id": "n1",
      "type": "trigger.command",
      "config": { "command": "/start" }
    },
    {
      "id": "n2",
      "type": "message",
      "config": {
        "text": "Привет, {{user.name}}!",
        "parseMode": "Markdown"
      }
    }
  ]
}
```

### Пример 2: Условие с переменной
```json
{
  "id": "n3",
  "type": "condition",
  "config": {
    "expression": "{{user.balance}} > 1000"
  }
}
```

### Пример 3: API запрос с маппингом
```json
{
  "id": "n4",
  "type": "action.api_request",
  "config": {
    "url": "https://api.example.com/users/{{user.id}}",
    "method": "GET",
    "responseMapping": {
      "bonusAmount": "bonus"
    }
  }
}
```

---

*Документ обновлен: 2025-01-30*
*Версия конструктора: 2.0*

