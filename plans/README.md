# Implementation Plans — b2b / referral subsystem

Generated by the improve skill on 2026-06-23, against commit `1830d2a`.
Scope: the b2b partner-hierarchy and referral-commission functionality.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Make the Tilda webhook order→bonus path idempotent | P1 | M | — | DONE (commit `d04cabd`, branch `advisor/001-webhook-idempotency`) |
| 002  | Make the multi-level referral payout atomic and observable | P1 | M | 001 (stacked) | DONE (commit `464e18b`, branch `advisor/002-payout-chain-transaction`) |
| 003  | Reverse purchase bonus & referral commission on refund/cancel | P1 | M | 001, 002 (stacked) | DONE (commit `0c24de2`, branch `advisor/003-refund-reversal`) |
| 004  | Reconcile commission depth (levels vs maxPayoutDepth vs UI) | P2 | S | — | TODO |
| 005  | Replace payout-chain heuristic with explicit parent links | P2 | L | 004 | TODO |
| 006  | Spike — real partner payout / withdrawal flow | P3 | L (spike) | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Recommended path

1. **001** first — highest single-fix value, independent, stops double-payments now.
2. **002** — foundation for reliable payouts; touches the same `processReferralBonus` method as 001, so land 001 first to avoid merge churn.
3. **003** — refund reversal; reuses the deterministic `externalId` from 001 and the observable-failure pattern from 002.
4. **004** — cheap consistency fix; do before 005 so depth semantics are settled before chain resolution is rewritten.
5. **005** — removes the wrong-manager payout risk; larger, may need a migration.
6. **006** — design spike for the biggest product gap (money-out for partners); output is a design doc that becomes a future build plan (007).

## Dependency notes

- 002 requires no code from 001 but edits the same function — sequence them to avoid conflicts.
- 003 requires 001 (deterministic `externalId` to locate what to reverse) and 002 (atomic-write + observable-failure pattern).
- 005 requires 004 (consistent depth) before rewriting chain resolution.

## Execution log

- **001 — DONE & reviewed (APPROVE), 2026-06-23.** Executor commit `d04cabd` on branch `advisor/001-webhook-idempotency`, worktree `.claude/worktrees/agent-ac9f47be4059a2175` (not merged/pushed — merging is the user's call). Reviewer-verified: early existence guard in `processOrder` (gated on non-empty `orderId`), deterministic `externalId` (`tilda_order_*`) on the EARN bonus/transaction with P2002 tolerance, per-payout `externalId` (`referral_<order>_<referrer>_L<level>`) in `processReferralBonus(userId, amount, orderId?)`, 4 new passing tests. **Caveat:** the two absolute done-criteria (`tsc --noEmit` exit 0, full services suite green) are unreachable on this repo's **pre-existing red baseline** (72 tsc errors, 9 failing tests on `1830d2a` before any change) — independently confirmed the change adds **zero** new tsc errors and zero test regressions. Minor follow-up: idempotency was threaded via local `as CreateBonusInput & { externalId? }` / `as CreateTransactionInput` casts because `src/types/bonus.ts` was out of scope; a future cleanup should add `externalId?` to those input types and drop the casts.

- **002 — DONE & reviewed (APPROVE), 2026-06-23.** Executor commit `464e18b` on branch `advisor/002-payout-chain-transaction`, **stacked on 001** (`d04cabd` is an ancestor — confirmed via `git merge-base --is-ancestor`), worktree `.claude/worktrees/agent-a67b4081945503ee0` (not merged/pushed). Reviewer-verified: `paidPayouts` hoisted out of the try so the outer catch reports partial state; non-P2002 errors annotated with `failedAtLevel` and re-thrown; the P2002 "already paid" path from 001 is preserved and correctly does NOT count as failure; outer catch emits a structured `logger.error` (paidLevels/failedAtLevel/per-payout detail) and returns `{ bonusAwarded:false, partialFailure }` without throwing. 2 new passing tests; tsc 72=72 (zero new); full services suite 9 failed/186 passed = baseline 9 failures + 2 new passes (zero regressions). **Deviation (plan-sanctioned):** used the plan's "lighter approach" — payouts are NOT transactionally atomic (a committed level-1 payout is not rolled back if level-2 fails); the delivered value is *observability + abort + reconcilable logging*, not rollback. True atomicity needs the deferred `awardBonus` money-only extraction (out of scope here). Acceptable: the previously-silent inconsistent state is now loud and reconcilable, and plan 003's reversal infra covers cleanup.

- **003 — DONE & reviewed (APPROVE), 2026-06-23.** Executor commit `0c24de2` on branch `advisor/003-refund-reversal`, **stacked on 002** (`464e18b` is an ancestor — confirmed), worktree `.claude/worktrees/agent-a3ef3d81101a84747` (not merged/pushed). Full stack: `1830d2a → d04cabd(001) → 464e18b(002) → 0c24de2(003)`. Reviewer-verified: `BonusService.reversePurchaseBonus` + `ReferralService.reverseReferralBonus` locate awards by the 001 externalIds (`tilda_order_*`, `referral_*`), mark source `Bonus.isUsed=true` and write a `REFUND` transaction inside `db.$transaction` (no network calls inside the tx); idempotent via `reversal_*` externalIds + P2002 catch; "already spent" reverses only the remaining amount and logs the shortfall (balance never negative); `changeOrderStatus`/`deleteOrder` trigger clawback only when entering `CANCELLED`/`REFUNDED` from a non-clawback status, each reversal in its own try/catch so a failure can't break the status update. 8 new meaningful tests pass; tsc 72=72 (zero new); full services suite 9 failed/194 passed = baseline 9 + 8 new passes (zero regressions). **Caveats (non-blocking):** (a) commit subject has a stray `@ ` prefix (cosmetic heredoc artifact — amend subject before merge if desired); (b) the repo's `lint-staged`/prettier pre-commit reformatted trailing commas across `order.service.ts`, inflating that file's diff (~127 cosmetic deletions) and widening merge-conflict surface — the logical change is only the two imports + the clawback wiring block.

## Findings considered and rejected

- **IDOR on partner API routes** (`team/[subjectUserId]`, `referral-insights/[subjectUserId]`): not a finding. Verified these are admin-gated (`getCurrentAdmin` + `ProjectService.verifyProjectAccess`) and additionally enforce `cachedCanViewSubject`. No ownership bypass.
- **МойСклад refund handling duplicated by plan 003**: avoided — МойСклад has its own return path via `Transaction.moySkladSaleId`; plan 003 is explicitly scoped to the Tilda/generic path and excludes МойСклад.

## Product/scenario gaps captured as direction (not yet planned)

These were raised in the audit and are tracked for the maintainer; only D1
(payout flow) became a plan (006). The rest await prioritization:

- **Partner lifecycle**: self-serve onboarding, deactivation/orphan reassignment, reparenting.
- **РФ tax/self-employment (НПД)** for commission income (partially covered inside 006's decision list).
- **PII privacy between partners**: a manager sees full contact details of all sub-trainers' clients in the bot — no masking.
- **Director/owner analytics**: plan effectiveness (paid vs revenue generated), leaderboard, dormant-partner detection.
- **Opt-out UI** for b2b notifications (today only editable via Prisma Studio / metadata).
