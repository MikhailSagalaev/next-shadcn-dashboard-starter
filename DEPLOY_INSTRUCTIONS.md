# 🚀 Инструкции для деплоя рефакторинга WidgetSettings

**Дата:** 2026-01-11  
**Коммит:** `09d33a9` - refactor: separate WidgetSettings from BotSettings

## ⚠️ ВАЖНО: Breaking Changes

Этот рефакторинг содержит breaking changes для API endpoints виджета.

## 📋 Шаги деплоя на сервере

### 1. Остановить приложение
```bash
pm2 stop all
# или
pm2 stop next-shadcn-dashboard-starter
```

### 2. Получить изменения
```bash
cd /path/to/project
git pull origin main
```

### 3. Установить зависимости (если нужно)
```bash
yarn install
```

### 4. Применить миграцию БД
```bash
# Сгенерировать Prisma Client с новой моделью
npx prisma generate

# Применить миграцию к БД
npx prisma migrate deploy
```

**Ожидаемый результат:**
```
Applying migration `20260111232432_add_widget_settings`
The following migration(s) have been applied:
migrations/
  └─ 20260111232432_add_widget_settings/
      └─ migration.sql
```

### 5. (Опционально) Мигрировать существующие данные
Если у вас уже есть проекты с настройками виджета в `BotSettings.functionalSettings.widgetSettings`:

```bash
npx tsx scripts/migrate-widget-settings.ts
```

**Примечание:** Если данных нет, скрипт выведет "0 записей мигрировано" - это нормально.

### 6. Собрать приложение
```bash
yarn build
```

### 7. Запустить приложение
```bash
pm2 start ecosystem.config.cjs
# или
pm2 restart all
```

### 8. Проверить логи
```bash
pm2 logs
```

## ✅ Проверка работоспособности

### 1. Проверить API endpoint
```bash
# Замените YOUR_PROJECT_ID на реальный ID проекта
curl https://your-domain.com/api/projects/YOUR_PROJECT_ID/widget
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "id": "...",
  "projectId": "...",
  "productBadgeBonusPercent": 10,
  "operationMode": "WITH_BOT",
  "botUsername": "your_bot",
  ...
}
```

### 2. Проверить админ-панель
1. Открыть админ-панель → Проект → Интеграция с Tilda
2. Изменить любую настройку виджета
3. Нажать "Сохранить настройки"
4. Обновить страницу и проверить что настройки сохранились

### 3. Проверить виджет на сайте
1. Открыть сайт с виджетом Tilda
2. Открыть DevTools → Console
3. Проверить что нет ошибок загрузки
4. Проверить что процент начисления корректный

## 🔧 Откат (если что-то пошло не так)

### Откатить миграцию БД
```bash
# Откатить последнюю миграцию
npx prisma migrate resolve --rolled-back 20260111232432_add_widget_settings

# Применить предыдущее состояние
git checkout HEAD~1
yarn build
pm2 restart all
```

### Восстановить старый endpoint
Если нужно временно вернуть старый endpoint `/max-bonus-percent`:
```bash
git revert 09d33a9
yarn build
pm2 restart all
```

## 📊 Мониторинг после деплоя

Следите за:
- ❌ Ошибками 404 на `/api/projects/*/widget`
- ❌ Ошибками в логах Prisma
- ❌ Ошибками в консоли браузера на сайтах с виджетом
- ✅ Корректной загрузкой настроек виджета
- ✅ Корректным процентом начисления бонусов

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs`
2. Проверьте статус миграций: `npx prisma migrate status`
3. Проверьте доступность БД: `npx prisma db pull`

## 📝 Что изменилось

### Новые файлы
- `prisma/migrations/20260111232432_add_widget_settings/migration.sql`
- `scripts/migrate-widget-settings.ts`
- `src/app/api/projects/[id]/widget/route.ts`
- `docs/widget-settings-refactoring.md`

### Изменённые файлы
- `prisma/schema.prisma` — добавлена модель WidgetSettings
- `src/middleware.ts` — добавлен публичный доступ к `/widget`
- `src/features/projects/components/tilda-integration-view.tsx` — использует `/widget`
- `public/tilda-bonus-widget.js` — использует `/widget` (v27)

### Удалённые файлы
- `src/app/api/projects/[id]/max-bonus-percent/route.ts`

## 🎯 Результат

После успешного деплоя:
- ✅ Настройки виджета хранятся в отдельной таблице `WidgetSettings`
- ✅ Один endpoint `/widget` вместо двух (`/bot` + `/max-bonus-percent`)
- ✅ Правильная архитектура с разделением ответственности
- ✅ Виджет работает с версией v27
