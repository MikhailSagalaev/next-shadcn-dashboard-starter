# Gupil Documentation

Пользовательская документация для SaaS платформы бонусных программ Gupil.

## 🚀 Запуск локально

```bash
# Установка зависимостей
yarn install

# Запуск в режиме разработки (http://localhost:3001)
yarn dev

# Сборка для продакшена
yarn build

# Экспорт статических файлов
yarn export
```

## 📁 Структура

```
user-docs/
├── pages/              # Страницы документации
│   ├── index.mdx       # Главная страница
│   ├── getting-started/
│   ├── admin-panel/
│   ├── telegram-bots/
│   ├── webhook-integration/
│   ├── bonus-system/
│   ├── workflow-constructor/
│   ├── api-reference/
│   └── faq.mdx
├── components/         # React компоненты
│   └── Layout.tsx      # Основной layout
├── styles/            # Стили
│   └── globals.css    # Глобальные стили
├── next.config.js     # Конфигурация Next.js
├── tailwind.config.js # Конфигурация Tailwind
└── package.json
```

## 📝 Добавление контента

1. Создайте новый `.mdx` файл в соответствующей папке
2. Используйте Layout компонент для единообразия
3. Добавьте ссылки в навигацию в `Layout.tsx`

## 🎨 Доступные стили

Документация использует Tailwind CSS для стилизации:

```mdx
# Заголовки автоматически стилизованы

## Подзаголовки тоже

Обычный текст с **жирным** и *курсивом*.

`Код в строке` и блоки кода:

```javascript
const example = "код";
```

> Цитаты выделены синим цветом

| Таблицы | Поддерживаются |
|---------|----------------|
| Да      | Конечно        |
```

## 🔗 Полезные ссылки

- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/)
- [MDX Documentation](https://mdxjs.com/)
- [Gupil Main Project](../README.md)

## 🚀 Деплой

Для деплоя статической версии:

1. Запустите `yarn build` для сборки
2. Используйте любой статический хостинг (Vercel, Netlify, GitHub Pages)
3. Или разместите файлы из папки `out/` на вашем сервере