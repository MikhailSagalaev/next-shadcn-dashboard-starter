# Конструктор сценариев (Workflow)

Визуальный конструктор позволяет создавать сложные сценарии взаимодействия с пользователями без написания кода. Вы соединяете блоки (ноды) линиями, определяя логику работы бота.

---

## Основные понятия

- **Нода (Node)** — отдельный блок сценария (триггер, сообщение, условие и т.д.)
- **Связь (Edge)** — линия, соединяющая ноды и определяющая порядок выполнения
- **Триггер** — точка входа в сценарий (команда, сообщение, callback)
- **Переменные** — данные, которые можно использовать в сообщениях: `{{user.balance}}`, `{{user.name}}`

---

## Типы клавиатур

### Inline клавиатура

Кнопки отображаются **под сообщением** и не занимают место системной клавиатуры.

**Возможности:**
- **URL** — открывает ссылку в браузере
- **Callback** — отправляет данные боту для обработки (используется для ветвления)

**Когда использовать:**
- Меню выбора действий
- Навигация по разделам бота
- Кнопки подтверждения/отмены

**Пример:** Кнопки "Получить подарок", "Мой баланс", "Связаться с поддержкой"

### Reply клавиатура

**Заменяет системную клавиатуру** телефона. Постоянно видна пока не уберете.

**Возможности:**
- **Обычный текст** — отправляет текст как сообщение пользователя
- **Запросить контакт** — пользователь делится номером телефона
- **Запросить геолокацию** — пользователь делится местоположением

**Когда использовать:**
- Регистрация (запрос телефона)
- Главное меню бота
- Сбор данных пользователя

**Пример:** Кнопка "Поделиться контактом" для регистрации

---

## Как создать ветвление по кнопкам

1. Добавьте ноду **"Сообщение"** или **"Inline клавиатура"**
2. В настройках добавьте клавиатуру типа **Inline**
3. Создайте кнопки с уникальными `callback_data`:
   - "Получить подарок" → `callback_data: get_gift`
   - "Подтвердить данные" → `callback_data: confirm_data`
4. Добавьте ноду **"Условие"** после сообщения
5. В условии проверяйте: `{{callback_data}} == "get_gift"`
6. Соедините ветки с нужными действиями

---

## Триггеры

Триггеры — это точки входа в сценарий. Каждый workflow должен начинаться с## Типы Нод (Блоков)

Конструктор состоит из блоков разных типов, каждый из которых выполняет свою функцию. Вы можете перетаскивать их из панели инструментов на холст.

### Таблица совместимости

| Тип Ноды | Иконка (в UI) | Назначение | Особенности |
| :--- | :---: | :--- | :--- |
| **Start** | 🟢 | Точка входа в сценарий. Срабатывает при старте бота командой `/start`. | Должна быть только одна на сценарий. |
| **Message** | 💬 | Отправляет пользователю текст, кнопки или медиа (фото/видео). | Самый часто используемый блок. |
| **Input** | ⌨️ | Ждет текстового или цифрового ввода от пользователя. | Сохраняет ответ в переменную. Можно настроить проверку (валидацию). |
| **Condition** | 🔀 | Разветвляет сценарий (Да/Нет) на основе условия. | Позволяет создавать умную логику и проверки. |
| **Action** | ⚡ | Выполняет скрытое действие: запрос к API, запись в базу, таймер. | Пользователь не видит этого шага, он происходит "под капотом". |
| **Command** | ⌨️ | Точка входа по любой команде (например, `/help`, `/settings`). | Альтернатива старту. Срабатывает в любой момент диалога. |
| **Callback** | 🖱️ | Срабатывает при нажатии на Inline-кнопку предыдущего сообщения. | Важно: `Callback Data` должна совпадать с настройкой кнопки. |
| **Middleware** | 🛡️ | Промежуточный фильтр (проверка доступа, авторизация). | Может заблокировать выполнение сценария, если условие не выполнено. |
| **Session** | 💾 | Работа с памятью бота (запомнить/вспомнить данные). | Хранит корзину товаров, временные выборы, состояние. |
| **End** | 🔴 | Явно завершает сценарий. | Рекомендуется ставить в конце веток. |

---

### Детальное описание нод

#### 1. Start (Старт) 🟢
Самая первая нода. Именно с неё начинается путь пользователя, когда он нажимает кнопку "Запустить" или пишет `/start`.
*   **Вход:** Нет (это начало).
*   **Выход:** Переход к первому сообщению.

#### 2. Message (Сообщение) 💬
Основной способ общения бота.
*   **Текст:** Поддерживает HTML форматирование (жирный, курсив, ссылки).
*   **Клавиатура:**
    *   **Inline:** Кнопки под сообщением. Могут вести на ссылки или вызывать действия (Callback).
    *   **Reply:** Кнопки вместо клавиатуры телефона. Удобно для главного меню.
*   **Вложения:** Можно прикрепить фото или документ по ссылке.

#### 3. Input (Ввод данных) ⌨️
Останавливает сценарий и ждет, пока пользователь что-то напишет.
*   **Тип данных:** Можно ждать текст, число, email или телефон.
*   **Валидация:** Бот сам переспросит, если пользователь введет некорректные данные (например, "Это не похоже на email").
*   **Переменная:** Ответ сохранится в переменную (например, `user_age`), которую можно использовать дальше: `{{user_age}}`.

#### 4. Condition (Условие) 🔀
"Мозг" вашего бота. Проверяет данные и пускает сценарий по одной из двух веток.
*   **Операторы:** Равно, Не равно, Больше, Меньше, Содержит.
*   **Пример:** Если `user_balance` > 1000, идем по зеленой ветке (предлагаем VIP услугу). Иначе — по красной.

#### 5. Action (Действие) ⚡
Сервисный блок для интеграций и логики.
*   **Database:** Найти пользователя в базе, проверить баланс.
*   **API Request:** Отправить данные во внешнюю CRM (Webhook).
*   **Delay:** Подождать N секунд (имитация печати).
*   **Variable:** Записать значение в переменную вручную.

#### 6. Command (Команда) ⌨️
Позволяет пользователю "перепрыгнуть" в эту часть сценария в любой момент, написав команду (например, `/menu`).

#### 7. Callback (Событие кнопки) 🖱️
Ловит нажатия на Inline-кнопки.
*   Создайте кнопку в блоке Message с `callback_data: 'buy_item'`.
*   Создайте ноду Callback и укажите там `buy_item`.
*   Соедините их (логически), и бот среагирует на нажатие.

#### 8. Middleware (Фильтр) 🛡️
Охранник на входе. Используется для проверки прав.
*   **Типы:** Logging (записать в лог), Auth (проверить авторизацию).
*   Если проверка не пройдена, сценарий пойдет по ветке "Ошибка" (Error).

#### 9. Session (Сессия) 💾
Продвинутая работа с данными.
*   Позволяет добавлять/удалять данные из временного хранилища.
*   Пример: "Добавить товар ID 55 в корзину".

#### 10. End (Конец) 🔴
Обозначает финиш сценария. Опционально может сбрасывать текущее состояние диалога.


---

## Логика

### Условие (`condition`)

Ветвление сценария по условию.

**Настройки:**
- **Выражение** — условие для проверки

**Операторы:**
- `==` — равно
- `!=` — не равно
- `>`, `<`, `>=`, `<=` — сравнение чисел
- `contains` — содержит подстроку
- `startsWith`, `endsWith` — начинается/заканчивается

**Примеры:**
```
{{user.balance}} > 100
{{callback_data}} == "confirm"
{{user.level}} == "VIP"
```

**Выходы:**
- **true** — условие выполнено
- **false** — условие не выполнено

### Switch (`flow.switch`)

Множественный выбор ветки по значению.

**Настройки:**
- **Переменная** — что проверять
- **Варианты** — список значений и соответствующих веток
- **Default** — ветка по умолчанию

**Пример:**
```
Переменная: {{callback_data}}
Варианты:
  - "balance" → показать баланс
  - "history" → показать историю
  - "help" → показать справку
  - default → неизвестная команда
```

---

## Управление потоком

### Задержка (`flow.delay`)

Пауза перед выполнением следующего шага.

**Настройки:**
- **Секунды** — время задержки

**Примеры использования:**
- Имитация "печатания"
- Отложенные напоминания
- Последовательные сообщения

### Цикл (`flow.loop`)

Повторяет действия заданное количество раз.

**Настройки:**
- **Количество итераций**
- **Переменная счетчика**

### Подпроцесс (`flow.sub_workflow`)

Запускает другой workflow как часть текущего.

**Настройки:**
- **Workflow ID** — какой сценарий запустить
- **Параметры** — данные для передачи

### Переход (`flow.jump`)

Безусловный переход на другую ноду.

**Настройки:**
- **Target Node ID** — куда перейти

### Завершение (`flow.end`)

Завершает выполнение сценария.

**Настройки:**
- **Статус** — success, error, cancelled

---

## Интеграции

### Webhook (`integration.webhook`)

Отправляет данные на внешний URL.

**Настройки:**
- **URL** — адрес webhook
- **Метод** — POST, GET
- **Данные** — что отправить

### Аналитика (`integration.analytics`)

Отправляет событие в систему аналитики.

**Настройки:**
- **Событие** — название события
- **Параметры** — дополнительные данные

---

## Примеры сценариев

### Приветствие нового пользователя

```
[Триггер: /start]
    ↓
[Сообщение: "Добро пожаловать! Выберите действие:"]
    + Inline кнопки:
      - "Получить подарок" (callback: get_gift)
      - "Мой баланс" (callback: balance)
    ↓
[Триггер: Callback]
    ↓
[Условие: callback_data == "get_gift"]
    ├── true → [Сообщение: "Ваш подарок: 100 бонусов!"]
    └── false → [Действие: Получить баланс] → [Сообщение: "Ваш баланс: {{balance}}"]
```

### Регистрация с запросом телефона

```
[Триггер: /start]
    ↓
[Действие: Проверка связи]
    ↓
[Условие: isLinked == true]
    ├── true → [Сообщение: "С возвращением, {{user.firstName}}!"]
    └── false → [Сообщение: "Поделитесь контактом для регистрации"]
                    + Reply кнопка: "Поделиться контактом" (request_contact)
                    ↓
                [Действие: Запрос контакта]
                    ↓
                [Действие: Поиск по контакту]
                    ↓
                [Условие: userFound]
                    ├── true → [Действие: Привязать Telegram]
                    └── false → [Сообщение: "Пользователь не найден"]
```

---

## История выполнения (Execution History)

Система предоставляет полную историю выполнения workflow в стиле n8n для отладки и мониторинга.

### Доступ к истории

1. Откройте workflow в конструкторе
2. Переключитесь на вкладку **"Executions"**
3. Выберите выполнение из списка

### Список выполнений

Таблица со всеми выполнениями workflow:

**Колонки:**
- **Статус** — running, completed, failed, waiting
- **Время начала** — когда запустилось выполнение
- **Длительность** — сколько времени заняло
- **Пользователь** — кто инициировал
- **Шаги** — количество выполненных шагов

**Фильтры:**
- По статусу (табы)
- По дате (date picker)
- Поиск по пользователю

### Визуализация пути выполнения

Canvas показывает путь выполнения с подсветкой нод:

- 🟢 **Зеленое свечение** — успешно выполненная нода
- 🔴 **Красное свечение** — нода с ошибкой
- 💫 **Пульсация** — нода в процессе выполнения
- ⚫ **Серый** — пропущенная нода

Анимированные edges показывают направление потока выполнения.

### Инспектор шага (StepInspector)

При клике на ноду открывается панель с полной информацией:

**Вкладки:**
- **Input** — входные данные шага (JSON)
- **Output** — результат выполнения (JSON)
- **Variables** — состояние переменных до/после выполнения
- **HTTP** — запросы/ответы для API нод
- **Error** — детали ошибки (если есть)

**Функции:**
- Копирование данных в буфер обмена
- Syntax highlighting для JSON
- Кнопка "Restart from here"

### Перезапуск с конкретной ноды

Возможность перезапустить workflow с любой ноды:

1. Выберите выполнение в списке
2. Кликните на ноду в визуализации
3. Нажмите **"Restart from here"** в инспекторе
4. Опционально включите **"Reset variables"** для сброса переменных
5. Подтвердите перезапуск

Новое выполнение будет связано с оригинальным через `parentExecutionId`.

### Real-time обновления

При просмотре активного выполнения:
- Автоматическое обновление статуса через SSE
- Подсветка текущей ноды в реальном времени
- Обновление списка шагов по мере выполнения

### Хранение данных

- История хранится **7 дней**
- Автоматическая очистка через cron job (`/api/cron/cleanup-executions`)
- Чувствительные данные (токены, пароли) автоматически маскируются

---

## Советы

1. **Начинайте с простого** — создайте базовый сценарий и постепенно усложняйте
2. **Используйте переменные** — они делают сообщения персонализированными
3. **Тестируйте по частям** — проверяйте каждую ветку отдельно
4. **Добавляйте fallback** — обрабатывайте неожиданные ответы пользователей
5. **Документируйте** — давайте понятные названия нодам
