# Обзор аналитики рассылок Telegram

## ✅ Что уже реализовано

### 1. База данных (Prisma Schema)

#### Основные таблицы:

**Mailing** - Основная таблица рассылок
- `id`, `projectId`, `name`, `type`, `status`
- Счетчики: `sentCount`, `openedCount`, `clickedCount`, `failedCount`
- Временные метки: `scheduledAt`, `sentAt`, `completedAt`
- Связи: `recipients`, `history`, `linkClicks`, `links`

**MailingRecipient** - Получатели рассылки
- `id`, `mailingId`, `userId`, `email`, `phone`, `telegramId`
- Статус: `status` (PENDING, SENT, FAILED, BOUNCED, DELIVERED)
- Временные метки: `sentAt`, `deliveredAt`, `openedAt`, `clickedAt`
- Счетчики: `openCount`, `clickCount`
- Метаданные: `error`, `metadata`

**MailingHistory** - История событий
- `id`, `mailingId`, `userId`, `recipientId`
- Тип события: `type` (MailingEventType)
- Временная метка: `timestamp`
- Метаданные: `metadata`

**MailingLinkClick** - Клики по ссылкам
- `id`, `mailingId`, `recipientId`, `url`
- Временная метка: `clickedAt`
- Технические данные: `userAgent`, `ipAddress`

**MailingLink** - Короткие ссылки для отслеживания
- `id`, `mailingId`, `originalUrl`, `shortCode`
- Связь с кликами через `MailingHistory`

**MailingTemplate** - Шаблоны рассылок
- `id`, `projectId`, `name`, `subject`, `body`
- `type`, `isActive`

### 2. API Endpoints

#### GET /api/projects/[id]/mailings/[mailingId]/analytics
Получение аналитики рассылки
- Статистика: отправлено, открыто, кликов, ошибок
- Процентные показатели: openRate, clickRate
- Временная шкала событий

#### GET /api/projects/[id]/mailings/[mailingId]/history
История событий рассылки
- Все события с фильтрацией
- Лимит записей
- Детальная информация о каждом событии

### 3. UI Компоненты

#### MailingAnalyticsView
**Путь:** `src/features/mailings/components/mailing-analytics-view.tsx`

**Функционал:**
- 📊 Карточки с основными метриками (отправлено, открыто, кликов, ошибок)
- 📈 Графики динамики событий (LineChart)
- 👥 Таблица получателей с детальной статистикой
- 📜 История всех событий рассылки
- 🔄 Табы для разных разделов аналитики

**Метрики:**
- Отправлено / Всего получателей
- Открыто + процент открытий
- Кликов + процент кликов
- Ошибок + процент ошибок

### 4. Типы событий (MailingEventType)

```typescript
enum MailingEventType {
  SENT          // Отправлено
  DELIVERED     // Доставлено
  OPENED        // Открыто
  CLICKED       // Клик по ссылке
  BOUNCED       // Отскок
  FAILED        // Ошибка
  UNSUBSCRIBED  // Отписка
}
```

## 📊 Доступные метрики

### Основные показатели
- ✅ Количество отправленных сообщений
- ✅ Количество доставленных сообщений
- ✅ Количество открытий
- ✅ Количество кликов
- ✅ Количество ошибок
- ✅ Процент открытий (Open Rate)
- ✅ Процент кликов (Click Rate)
- ✅ Процент ошибок (Error Rate)

### Детальная аналитика
- ✅ Статистика по каждому получателю
- ✅ История всех событий с временными метками
- ✅ Отслеживание кликов по ссылкам
- ✅ Технические данные (User Agent, IP)
- ✅ Метаданные событий

### Временная аналитика
- ✅ График динамики отправки
- ✅ График динамики открытий
- ✅ График динамики кликов
- ✅ Временные метки всех событий

## 🎯 Что работает для Telegram

### Отслеживание отправки
- ✅ Статус отправки (PENDING, SENT, FAILED)
- ✅ Временная метка отправки
- ✅ Ошибки отправки с деталями

### Отслеживание доставки
- ✅ Статус доставки (DELIVERED)
- ✅ Временная метка доставки
- ✅ Telegram ID получателя

### Отслеживание взаимодействий
- ⚠️ Открытия (ограничено в Telegram)
- ✅ Клики по кнопкам (через callback_data)
- ✅ Клики по ссылкам (через короткие ссылки)

## ⚠️ Ограничения Telegram

### Что НЕ работает в Telegram:
1. **Отслеживание открытий** - Telegram не предоставляет информацию об открытии сообщений
2. **Пиксели отслеживания** - Нельзя встроить невидимые изображения для трекинга
3. **Email-метрики** - Специфичные для email показатели недоступны

### Что РАБОТАЕТ в Telegram:
1. ✅ **Отправка и доставка** - Telegram API возвращает статус отправки
2. ✅ **Клики по кнопкам** - Через callback_query можно отслеживать нажатия
3. ✅ **Клики по ссылкам** - Через короткие ссылки с редиректом
4. ✅ **Ошибки отправки** - Заблокированные боты, неверные ID и т.д.

## 🔧 Как использовать

### Просмотр аналитики
1. Перейдите в раздел "Рассылки"
2. Выберите рассылку
3. Нажмите кнопку "Аналитика"
4. Изучите метрики и графики

### Отслеживание кликов по ссылкам
```typescript
// При создании рассылки включите отслеживание ссылок
const mailing = await MailingService.create({
  linkTrackingEnabled: true,
  messageText: 'Перейдите по ссылке: https://example.com'
});

// Система автоматически создаст короткие ссылки
// и будет отслеживать клики
```

### Отслеживание кликов по кнопкам
```typescript
// Добавьте кнопки с callback_data
const keyboard = {
  inline_keyboard: [[
    {
      text: 'Нажми меня',
      callback_data: 'button_clicked'
    }
  ]]
};

// В обработчике callback_query сохраняйте событие
bot.on('callback_query', async (ctx) => {
  await MailingService.trackClick({
    mailingId: 'xxx',
    recipientId: 'yyy',
    type: 'CLICKED',
    metadata: { button: ctx.callbackQuery.data }
  });
});
```

## 📈 Примеры использования

### Получение статистики рассылки
```typescript
const stats = await MailingService.getMailingStats(mailingId);

console.log({
  sent: stats.sent,
  delivered: stats.delivered,
  clicked: stats.clicked,
  failed: stats.failed,
  openRate: stats.openRate,
  clickRate: stats.clickRate
});
```

### Получение истории событий
```typescript
const history = await MailingService.getMailingHistory(mailingId, {
  limit: 100,
  type: 'CLICKED' // Фильтр по типу события
});
```

### Отслеживание получателей
```typescript
const recipients = await db.mailingRecipient.findMany({
  where: { mailingId },
  include: {
    user: true,
    linkClicks: true,
    history: true
  }
});
```

## 🚀 Рекомендации по улучшению

### Для Telegram рассылок:

1. **Используйте кнопки вместо ссылок**
   - Кнопки с callback_data отслеживаются лучше
   - Можно получить точную статистику кликов

2. **Включайте короткие ссылки**
   - Для отслеживания переходов по URL
   - Автоматическое создание через `linkTrackingEnabled: true`

3. **Сохраняйте метаданные**
   - Telegram ID пользователя
   - Информация о боте
   - Контекст отправки

4. **Отслеживайте ошибки**
   - Заблокированные боты
   - Неверные Telegram ID
   - Ошибки API

5. **Используйте сегментацию**
   - Создавайте сегменты пользователей
   - Анализируйте эффективность по сегментам
   - A/B тестирование разных сообщений

## 📊 Доступные отчеты

### В UI доступны:
- ✅ Обзор рассылки (общая информация)
- ✅ Графики динамики событий
- ✅ Таблица получателей с детальной статистикой
- ✅ История всех событий

### Можно добавить:
- 📊 Сравнение рассылок
- 📈 Тренды по времени
- 🎯 Анализ по сегментам
- 💰 ROI рассылок
- 🔥 Тепловая карта активности
- 📱 Статистика по устройствам (если доступно)

## 🔗 Связанные файлы

### Компоненты:
- `src/features/mailings/components/mailing-analytics-view.tsx`
- `src/features/mailings/components/mailings-page-view.tsx`

### API:
- `src/app/api/projects/[id]/mailings/[mailingId]/analytics/route.ts`
- `src/app/api/projects/[id]/mailings/[mailingId]/history/route.ts`

### Сервисы:
- `src/lib/services/mailing.service.ts`

### База данных:
- `prisma/schema.prisma` (модели Mailing, MailingRecipient, MailingHistory, etc.)

---

**Статус:** ✅ Полностью реализовано и работает  
**Версия:** 1.0  
**Дата:** 31 января 2025
