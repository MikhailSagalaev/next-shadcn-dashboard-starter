# Как посмотреть аналитику рассылок

## 📍 Путь к аналитике рассылок

### Вариант 1: Через интерфейс (рекомендуется)

1. **Откройте проект**
   ```
   Главное меню → Проекты → Выберите нужный проект
   ```

2. **Перейдите в раздел "Рассылки"**
   ```
   URL: /dashboard/projects/{projectId}/mailings
   ```
   
   Или через меню проекта (если есть вкладка "Рассылки")

3. **Найдите нужную рассылку в таблице**
   - В таблице отображаются все рассылки проекта
   - Показываются: название, тип, статус, дата создания

4. **Откройте меню действий**
   - Нажмите на иконку с тремя точками (⋮) или иконку письма в строке рассылки
   - В выпадающем меню выберите **"Статистика"** (иконка графика 📊)

5. **Просмотр аналитики**
   ```
   URL: /dashboard/projects/{projectId}/mailings/{mailingId}/analytics
   ```

### Вариант 2: Прямая ссылка

Если вы знаете ID проекта и ID рассылки:

```
https://your-domain.com/dashboard/projects/{projectId}/mailings/{mailingId}/analytics
```

Пример:
```
https://your-domain.com/dashboard/projects/clx123abc/mailings/clm456def/analytics
```

## 📊 Что вы увидите на странице аналитики

### 1. Основные метрики (карточки вверху)

```
┌─────────────┬─────────────┬─────────────┬─────────────┐
│ Отправлено  │   Открыто   │   Кликов    │   Ошибок    │
│    1,234    │     856     │     342     │      12     │
│ из 1,246    │   69.3%     │   27.7%     │    1.0%     │
└─────────────┴─────────────┴─────────────┴─────────────┘
```

### 2. Табы с детальной информацией

#### Вкладка "Обзор"
- Тип рассылки
- Статус
- Дата создания
- Дата отправки

#### Вкладка "Графики"
- График динамики отправки
- График открытий
- График кликов
- Временная шкала событий

#### Вкладка "Получатели"
Таблица со всеми получателями:
- Email/Телефон/Telegram ID
- Статус доставки
- Время открытия
- Количество кликов
- Время отправки

#### Вкладка "История"
Все события рассылки:
- Тип события (SENT, DELIVERED, CLICKED, etc.)
- Получатель
- Время события
- Дополнительные детали

## 🔍 Как найти ID рассылки

### Способ 1: Через URL
Когда вы находитесь на странице рассылок, ID виден в URL:
```
/dashboard/projects/clx123abc/mailings
                    ↑ это ID проекта
```

### Способ 2: Через API
```bash
# Получить список всех рассылок проекта
curl https://your-domain.com/api/projects/{projectId}/mailings
```

### Способ 3: Через базу данных
```sql
SELECT id, name, status, created_at 
FROM mailings 
WHERE project_id = 'your-project-id'
ORDER BY created_at DESC;
```

## 🚀 Быстрый доступ

### Если рассылки нет в меню проекта

Добавьте прямую ссылку в закладки браузера:
```
/dashboard/projects/{ваш-project-id}/mailings
```

### Создание рассылки

1. Перейдите на страницу рассылок
2. Нажмите кнопку **"+ Создать рассылку"**
3. Заполните форму:
   - Название
   - Тип (EMAIL, SMS, TELEGRAM)
   - Сегмент получателей
   - Текст сообщения
4. Сохраните и запустите

## 📱 Структура URL проекта

```
/dashboard/projects/{projectId}/
├── overview          # Обзор проекта
├── users             # Пользователи
├── bonuses           # Бонусы
├── levels            # Уровни
├── settings          # Настройки
├── analytics         # Аналитика
├── logs              # Логи
├── mailings          # 📧 РАССЫЛКИ
│   ├── /             # Список рассылок
│   ├── /{mailingId}  # Детали рассылки
│   └── /{mailingId}/analytics  # 📊 АНАЛИТИКА
├── segments          # Сегменты
├── orders            # Заказы
├── products          # Товары
├── chats             # Чаты
├── bots              # Боты
├── workflows         # Workflow
└── integrations      # Интеграции
```

## 🎯 Примеры использования

### Пример 1: Просмотр статистики последней рассылки

1. Откройте `/dashboard/projects/{projectId}/mailings`
2. Первая рассылка в списке - самая последняя
3. Нажмите на меню (⋮) → "Статистика"

### Пример 2: Сравнение нескольких рассылок

1. Откройте аналитику первой рассылки
2. Запишите метрики (открытия, клики)
3. Вернитесь назад
4. Откройте аналитику второй рассылки
5. Сравните показатели

### Пример 3: Экспорт данных

На странице аналитики:
1. Перейдите на вкладку "Получатели"
2. Скопируйте данные из таблицы
3. Или используйте API для экспорта:

```bash
curl https://your-domain.com/api/projects/{projectId}/mailings/{mailingId}/analytics
```

## 🔧 Если не видите рассылки

### Проверьте права доступа
- Убедитесь, что вы администратор проекта
- Проверьте, что проект активен

### Проверьте наличие рассылок
```sql
SELECT COUNT(*) FROM mailings WHERE project_id = 'your-project-id';
```

### Создайте тестовую рассылку
1. Перейдите на `/dashboard/projects/{projectId}/mailings`
2. Нажмите "+ Создать рассылку"
3. Заполните минимальные данные
4. Сохраните как черновик
5. Теперь она появится в списке

## 📞 Поддержка

Если у вас возникли проблемы:

1. **Проверьте URL** - убедитесь, что используете правильный ID проекта
2. **Проверьте права** - у вас должны быть права администратора
3. **Проверьте логи** - откройте консоль браузера (F12)
4. **Проверьте API** - попробуйте прямой запрос к API

---

**Быстрая ссылка для копирования:**
```
/dashboard/projects/ВАШ_PROJECT_ID/mailings
```

Замените `ВАШ_PROJECT_ID` на реальный ID вашего проекта!
