# 📝 Telegram Rich Editor

> WYSIWYG редактор для Telegram сообщений с поддержкой переменных и HTML форматирования

## 🎯 Быстрый старт

```tsx
import { TelegramRichEditor } from '@/components/ui/telegram-rich-editor';

function MyComponent() {
  const [message, setMessage] = useState('');

  return (
    <TelegramRichEditor
      value={message}
      onChange={setMessage}
      placeholder="Введите текст сообщения..."
      showVariableHelper={true}
    />
  );
}
```

## ✨ Возможности

- 🎨 **WYSIWYG редактирование** - визуальное форматирование текста
- ⌨️ **Горячие клавиши** - Ctrl+B, Ctrl+I, Ctrl+U
- 🔗 **Ссылки** - удобная вставка через popup
- 📊 **Переменные** - динамические данные пользователя
- ↶ **История** - отмена/повтор действий
- 📏 **Счетчики** - символов и переменных
- 🔒 **Безопасность** - только Telegram-совместимые теги

## 📍 Где используется

| Место | Файл | Описание |
|-------|------|----------|
| Workflow конструктор | `workflow-properties.tsx` | Редактирование сообщений |
| Workflow конструктор | `message-node.tsx` | Превью сообщений |
| Бот-конструктор | `bot-constructor-properties.tsx` | Редактирование сообщений бота |
| Система бонусов | `rich-notification-dialog.tsx` | Массовые уведомления |

## 🎨 Форматирование

| Формат | HTML | Клавиши |
|--------|------|---------|
| **Жирный** | `<b>` | Ctrl+B |
| *Курсив* | `<i>` | Ctrl+I |
| <u>Подчеркнутый</u> | `<u>` | Ctrl+U |
| ~~Зачеркнутый~~ | `<s>` | - |
| `Код` | `<code>` | - |
| Ссылка | `<a>` | 🔗 |

## 📦 API

```tsx
interface TelegramRichEditorProps {
  value: string;                    // HTML строка
  onChange: (value: string) => void; // Callback
  placeholder?: string;              // Placeholder
  className?: string;                // CSS классы
  showVariableHelper?: boolean;      // Показать кнопку переменных
  minHeight?: string;                // Минимальная высота
}
```

## 🔧 Примеры

### С переменными
```tsx
<TelegramRichEditor
  value={message}
  onChange={setMessage}
  showVariableHelper={true}
/>
```

### Кастомная высота
```tsx
<TelegramRichEditor
  value={message}
  onChange={setMessage}
  minHeight="300px"
/>
```

### Без переменных
```tsx
<TelegramRichEditor
  value={message}
  onChange={setMessage}
  showVariableHelper={false}
/>
```

## 📚 Документация

- 📖 [Руководство пользователя](TELEGRAM_EDITOR_GUIDE.md)
- 🔧 [Техническая документация](docs/telegram-rich-editor.md)
- 📋 [История изменений](CHANGELOG_TELEGRAM_EDITOR.md)
- 📊 [Краткая сводка](TELEGRAM_EDITOR_SUMMARY.md)

## 🚀 Запуск

```bash
# Установка зависимостей
yarn install

# Запуск dev сервера
yarn dev

# Сборка production
yarn build
```

## 🧪 Тестирование

1. Откройте http://localhost:5006
2. Перейдите в Workflow конструктор
3. Создайте ноду "Сообщение"
4. Попробуйте форматирование и переменные

## 💡 Советы

- Используйте **Ctrl+B/I/U** для быстрого форматирования
- Нажмите **"Переменные"** для вставки динамических данных
- Используйте **Ctrl+Z/Y** для отмены/повтора
- Выделите текст и нажмите **🔗** для вставки ссылки

## 🐛 Troubleshooting

### Редактор не отображается
```bash
yarn install
```

### Форматирование не работает
Убедитесь, что `parseMode: 'HTML'`

### Переменные не вставляются
Проверьте `showVariableHelper={true}`

## 📞 Поддержка

- 📖 Читайте документацию
- 🐛 Создайте issue
- 💬 Спросите в чате

## 📄 Лицензия

MIT

---

**Версия:** 1.0.0  
**Дата:** 2025-11-29  
**Статус:** ✅ Production Ready
